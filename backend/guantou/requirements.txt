# Core Django
django==6.1
django-cors-headers~=4.9.0
django-simpleui==2026.1.13
djangorestframework~=3.18.0

# Utilities
demjson3~=3.0.6
pyjwt~=2.4.0
django-environ==0.14.0

# Data Processing
numpy>=2.5.3,<3.0.0  # Python 3.12+ compatible, avoid numpy 2.x for compatibility
requests
requests_toolbelt==1.0.0

# Audio Processing
pydub==0.25.1
audioop-lts; python_version >= "3.13"
# Note: ffmpeg and ffprobe are system utilities, not Python packages
# Install via system package manager: apt-get install ffmpeg (Linux) or brew install ffmpeg (macOS)
# The obsolete Python packages 'ffmpeg==1.4' and 'ffprobe==0.5' have been removed

# Cloud Storage
cos-python-sdk-v5

# Development Tools
setuptools>=84.0.0,<85
black==26.5.1
